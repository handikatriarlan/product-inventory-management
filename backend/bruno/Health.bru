meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

tests {
  test("status 200 dan database up", function() {
    expect(res.status).to.equal(200)
    expect(res.body.data.status).to.equal("ok")
    expect(res.body.data.database).to.equal("up")
  })
}
