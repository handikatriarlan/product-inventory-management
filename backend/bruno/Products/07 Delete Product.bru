meta {
  name: Delete Product
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/products/{{productId}}
  body: none
  auth: none
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204)
  })
}
