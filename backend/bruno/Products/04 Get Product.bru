meta {
  name: Get Product
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/products/{{productId}}
  body: none
  auth: none
}

tests {
  test("status 200 dan id sesuai", function() {
    expect(res.status).to.equal(200)
    expect(res.body.data.id).to.equal(bru.getEnvVar("productId"))
  })
}
